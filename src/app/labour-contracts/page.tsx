"use client";
import AppContainer from "@/components/common/AppContainer";
import { Typography } from "@mui/material";

export default function LabourContractsPage() {
  return (
    <AppContainer>
      <Typography variant="h5" gutterBottom>
        Labour Contracts
      </Typography>
      {/* TODO: ContractForm + payouts table */}
    </AppContainer>
  );
}
