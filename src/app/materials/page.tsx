"use client";
import AppContainer from "@/components/common/AppContainer";
import { Typography } from "@mui/material";

export default function MaterialsPage() {
  return (
    <AppContainer>
      <Typography variant="h5" gutterBottom>
        Material Stock
      </Typography>
      {/* TODO: MaterialStockForm + stock ledger */}
    </AppContainer>
  );
}
