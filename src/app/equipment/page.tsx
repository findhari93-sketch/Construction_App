"use client";
import AppContainer from "@/components/common/AppContainer";
import { Typography } from "@mui/material";

export default function EquipmentPage() {
  return (
    <AppContainer>
      <Typography variant="h5" gutterBottom>
        Equipment Tracker
      </Typography>
      {/* TODO: EquipmentForm + status table */}
    </AppContainer>
  );
}
